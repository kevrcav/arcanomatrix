local event = require 'event'
local eventmanager = require 'eventmanager'
local listener = require 'listener'
local vector = require 'vector'

local matrixzone = {topleft = vector:vect0(), size = vector:vect0()}

return matrixzone
